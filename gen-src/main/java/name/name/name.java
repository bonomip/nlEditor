package name.name;
import neverlang.runtime.*;
public class name extends Slice {
  public name() {
    importSyntax("neverlang.commons.expressions.bitwise.or.BitwiseOrOperand");
        imports("neverlang.commons.expressions.unary.ParenthesizedExpression", 
         role("translate"));

  }
}
